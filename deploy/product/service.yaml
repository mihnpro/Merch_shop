apiVersion: v1
kind: Service
metadata:
  name: product
  namespace: merchshop
  labels:
    app: product
spec:
  type: ClusterIP
  selector:
    app: product
  ports:
    - name: http
      port: 8082
      targetPort: 8082
    - name: grpc
      port: 50051
      targetPort: 50051
