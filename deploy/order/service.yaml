apiVersion: v1
kind: Service
metadata:
  name: order
  namespace: merchshop
  labels:
    app: order
spec:
  type: ClusterIP
  selector:
    app: order
  ports:
    - name: http
      port: 8086
      targetPort: 8086
    - name: grpc
      port: 50056
      targetPort: 50056
